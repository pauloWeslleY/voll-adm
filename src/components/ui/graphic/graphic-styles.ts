import { styled } from '@mui/material'

export const GraphicWrap = styled('section')(({ theme }) => ({
  background: theme.palette.common.white,
  borderRadius: theme.spacing(2.6),

  marginTop: theme.spacing(2),
}))
